1. デフォルトで書き出す
2. 書き出す前に、現在のzodの型との整合性をチェックする
3. 同じ場合はそのまま、さらに追加されていた場合は追加することもできる。（これはオプション）

update mode
すでに手動で変更したスキーマを変更されてはたまらん。
よって、

mysql-to-zod generate 
mysql-to-zod update

1行ずつ読んで、export constが   入っている行がはじまったら
read開始
そのあと「;」が見つかったらリード終了。

そして最終的には

{
  tableName: "configCancel",
  properties: ["DB_ID", "GROUP_ID", "sort_key", "disp_cancel", "cancel_text"]
}
というオブジェクトの配列が取得できるようにする。

その後、実際にgenerateしてみて
足りないものを追加してあげる。というか
すでにあるものを後から上書きする感じか……。